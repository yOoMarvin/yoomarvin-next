// this is the main file for exporting the different kinds of buttons
// style contains all different buttons styles + the base style
// different components are gathered here and exported so you only need to import the Button Component

import OutlineButton from "./OutlineButton";
import GhostButton from "./GhostButton";
import FacebookButton from "./FacebookButton";
import TwitterButton from "./TwitterButton";
import CopyLinkButton from "./CopyLinkButton";

export {
  OutlineButton,
  GhostButton,
  FacebookButton,
  TwitterButton,
  CopyLinkButton
};
